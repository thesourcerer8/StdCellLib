name: CI

on:
  # Runs on Every Push
  push:
  # Runs on Pull Requests
  pull_request:
  workflow_dispatch:

jobs:
  pdk:
    runs-on: ubuntu-latest
    steps:

      - name: Test
        run: |
          ls

      #- name: Upload PDK Tarball
      #  uses: actions/upload-artifact@v2
      #  with:
      #    name: pdk-tarball
      #    path: /tmp/pdk.tar
