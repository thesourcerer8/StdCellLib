default:
  image: leviathanch/riscv-toolchain:latest

stages:
  - build

gf180_lib:
  stage: build
  script:
    - rm -f Tech && ln -s Tech.GF180MCU Tech
    - cd Catalog && make layout
    - cp -r Catalog/outputlib gf180_lib
  artifacts:
    paths:
      - gf180_lib
